
#include <iostream>
#include "utils/utils.h"
//Logger logger = initLogger();
int main() {
    logger.debug("Starting");
    
    int result = 100;
    remainder("主菜单");
    mainMenu();
    return 0;
}
